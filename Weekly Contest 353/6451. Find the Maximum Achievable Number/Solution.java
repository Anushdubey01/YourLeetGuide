class Solution {
    public int theMaximumAchievableX(int num, int t) {
        int maxAchievable = num + t + t;  // Initialize the maximum achievable number to num + 2*t
        
        return maxAchievable;
    }
}
